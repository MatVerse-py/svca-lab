[project]
name = "svca-lab"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "requests",
  "python-dotenv",
  "blake3",
  "gitpython",
  "pytest"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
